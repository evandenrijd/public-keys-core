#+TITLE: Changelog
#+OPTIONS: toc:nil

* Unreleased
** 2026/06/07 -- feat: publish ed25519 signing key AD8B718F7FB4C89A
- =data/signing-keyid-AD8B718F7FB4C89A.asc= (new, 1286 bytes) -- ~gpg --armor --export AD8B718F7FB4C89A~ output covering the ed25519 primary ~1605A39F6902DEDAC252AB825F4FBF967C1F9546~ (2026-05-26) and its three subkeys: [S] ~46DADF80CAF5C22CB746C598AD8B718F7FB4C89A~ (signing), [E] ~5D9507FD93A934A052E4A28CED328E57F333CE46~ (encryption), [A] ~096300008278BD8040D0DA34CAC523A4C34F9842~ (authentication).  The [S] subkey is the one ~~/vc/projects/env/expenses-core2/data/signature.pl.conf~ references when ~ec2-build~ signs invoice PDFs; the embedded PDF footer points readers at ~https://evandenrijd.github.io/public-keys-core~ for the .asc, which until now hosted only the legacy 2019 ~90149B1A71DC95FE~ rsa key -- so external verifiers (the employer downloading from the public site) couldn't match the new signatures.
- =README.org= ~* Public Keys~ table widened from one row to two: perso ed25519 (2026) on top, legacy rsa (2019) underneath.  Comment column rewritten from ~Signing key~ to ~Signing key (ed25519, 2026)~ + ~Legacy signing key (rsa, 2019)~ so the algorithm and vintage are visible at a glance.  Legacy entry retained -- historical PDFs signed pre-2026-05-26 (the ed25519 key's creation date) still require the rsa pubkey for verification, so removing it would break archival verifiability.
- =index.html= regenerated via ~make~ (which runs ~emacs --batch --eval '(require 'org)' README.org --funcall org-html-export-to-html~ and ~mv~'s the output).  Both keys verified present in the rendered HTML (~grep AD8B71~ + ~grep 90149B~ both hit).
- =CLAUDE.md= added (new) -- first agent-context doc for this repo.  Documents the static-site layout (data/ .asc files, README.org -> index.html via emacs batch HTML export), the ~make~ workflow, the add-a-new-key recipe (gpg export -> README table row -> make -> commit + push), and adopts the env-family ~update_changelog~ commit flow (which auto-bootstraps =CHANGELOG.org= on its first invocation -- this commit is that first invocation).  Pre-flow commits (~8b2d315~ initial through ~f0ba104~ openvpn cert) used a free-form ~Add: ...~ style and have no changelog entry -- ~* Unreleased~ starts here.  Consumers section enumerates the env subdirs that lean on this site (~expenses-core2~ PDF footer, ~gitops/~ commit verification, etc.) so agents touching this repo can predict downstream breakage.
- Bundled change motivated by ~env/FOLLOW_UP.org~ item 'Publish perso ed25519 [S] pub key to public-keys-core' (closed in env commit landing alongside this one).  Until pushed to ~origin/main~, GitHub Pages still serves the one-row version -- ~git push~ triggers Pages republish within ~1min and the ~ec2-build~ PDF footer URL becomes resolvable for external verifiers.

